from dependency_injector import providers, containers

class MLModels(containers.DeclarativeContainer):
    # decisionTree = providers.Factory(DecisionTree)
    print("--- Machine Learning Models Dependencies set ---")
    # other models